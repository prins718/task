const name = "Jesse";
const age = 40;

export {name, age};

console.log("----------------------------")

const message = () => {
const name1 = "Prins";
const age1 = 20;
return name1 + ' is ' + age1 + 'years old.';
};

export default message;